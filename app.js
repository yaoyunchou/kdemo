//app.js
const openIdUrl = require('./config').openIdUrl

App({
  onLaunch: function() {
    //调用API从本地缓存中获取数据
    // var logs = wx.getStorageSync('logs') || []
    // // logs.unshift(Date.now());
    // this.getUserInfo();
    // this.setUserInfo();
  }
})
